# Legal AI — Improvements Backlog

> Живий документ. Тут фіксуємо всі "достатньо для MVP, але можна краще".
> Поповнювати під час розробки, а не втрачати в чаті.

---

## 📇 Індекс (за номером)

> Нижче пункти згруповані **за темами**, а номери (`#N`) — це **стабільні ID**, видані в
> порядку появи ідеї (тому в тілі файлу вони не послідовні). На ці ID посилаються
> GitHub-issues та changelog — **не перенумеровувати**. Цей індекс дає читання по порядку.
> ⚠️ = ID-колізія (один номер на два пункти, треба розвести — див. кінець індексу).

| ID | Назва |
|----|-------|
| **#2** | [RAG з українськими законами — парсинг + векторизація](#2-rag-з-українськими-законами-парсинг-векторизація) |
| **#2а** | [Моніторинг змін у законах — повна архітектура](#2а-моніторинг-змін-у-законах-повна-архітектура) |
| **#2б** | [Агент-критик після генерації документу](#2б-агент-критик-після-генерації-документу) |
| **#3** | [Структура Google Doc — базовий шаблон](#3-структура-google-doc-базовий-шаблон) |
| **#4** | [Мінімізація галюцинацій](#4-мінімізація-галюцинацій) |
| **#4a** | [Старі посилання на форму в Telegram залишаються клікабельними](#4a-старі-посилання-на-форму-в-telegram-залишаються-клікабельними) |
| **#4b** | [Якість AI генерації — виявлені проблеми](#4b-якість-ai-генерації-виявлені-проблеми) |
| **#4с** | [Текст спільного майна — неінформативний бланк](#4с-текст-спільного-майна-неінформативний-бланк) |
| **#5** | [Відсутні `lawyer_type` і `revenue_share_percent`](#5-відсутні-lawyer_type-і-revenue_share_percent) |
| **#6** | [Таблиця `transactions` відсутня](#6-таблиця-transactions-відсутня) |
| **#7** | [При реєстрації юриста не створюється запис у `lawyers`](#7-при-реєстрації-юриста-не-створюється-запис-у-lawyers) |
| **#8** | [Помилка збереження — тихий fail](#8-помилка-збереження-тихий-fail) |
| **#9** | [Quality checklist в AI-промпт вкладці — тільки візуальний](#9-quality-checklist-в-ai-промпт-вкладці-тільки-візуальний) |
| **#10** | [Drag & drop полів у конструкторі](#10-drag-drop-полів-у-конструкторі) |
| **#10а** | [DatePickerField — навігація по роках незручна](#10а-datepickerfield-навігація-по-роках-незручна) |
| **#11** | [Form preview — ручний refresh](#11-form-preview-ручний-refresh) |
| **#11а** | [Адмін-панель — UX для юриста](#11а-адмін-панель-ux-для-юриста) |
| **#11б** | [Парсинг законів при створенні послуги — чому це важливо](#11б-парсинг-законів-при-створенні-послуги-чому-це-важливо) |
| **#12** | [Повноцінний Admin Dashboard (статистика + управління юристами)](#12-повноцінний-admin-dashboard-статистика-управління-юристами) |
| **#13** | [`supabase-migrations.sql` — ручне виконання](#13-supabase-migrationssql-ручне-виконання) |
| **#13а** | [Global Config нода — секрети в коді замість n8n Variables](#13а-global-config-нода-секрети-в-коді-замість-n8n-variables) |
| **#14** | [`vercel.json` rewrites — потенційний конфлікт](#14-verceljson-rewrites-потенційний-конфлікт) |
| **#15** | [Перейменування проекту `legal-twa` → `legal-ai`](#15-перейменування-проекту-legal-twa-legal-ai) |
| **#16** | [GDPR / Захист персональних даних](#16-gdpr-захист-персональних-даних) |
| **#17** | [Шаблони документів: від JS builder до Google Docs плейсхолдерів](#17-шаблони-документів-від-js-builder-до-google-docs-плейсхолдерів) |
| **#18** | [Service Builder — конструктор послуг для юристів](#18-service-builder-конструктор-послуг-для-юристів) |
| **#19** | [Архітектурна оцінка (станом на 2026-03-24)](#19-архітектурна-оцінка-станом-на-2026-03-24) |
| **#20** | [Service Builder — процес додавання нової послуги](#20-service-builder-процес-додавання-нової-послуги) |
| **#21** | [Створити Project Constitution для Legal-AI](#21-створити-project-constitution-для-legal-ai) |
| **#22** | [Писати Feature Spec перед кожною новою формою документа](#22-писати-feature-spec-перед-кожною-новою-формою-документа) |
| **#23** | [GraphRAG — граф зв'язків між законами (поетапно)](#23-graphrag-граф-звязків-між-законами-поетапно) |
| **#24** | [Інтерфейс для Ольги (HITL — Human-in-the-Loop)](#24-інтерфейс-для-ольги-hitl-human-in-the-loop) |
| **#25** | [AI-асистент для заповнення форми (Shared State)](#25-ai-асистент-для-заповнення-форми-shared-state) |
| **#26** | [Голосове заповнення форми через Telegram](#26-голосове-заповнення-форми-через-telegram) |
| **#27** | [Prompt Debt — версіонування промптів](#27-prompt-debt-версіонування-промптів) |
| **#28** | [Model Debt — абстракція моделей](#28-model-debt-абстракція-моделей) |
| **#29** | [Retrieval Debt — актуальність RAG](#29-retrieval-debt-актуальність-rag) |
| **#30** | [Control Debt — моніторинг якості генерації](#30-control-debt-моніторинг-якості-генерації) |
| **#31** | [Security plugin для всієї команди](#31-security-plugin-для-всієї-команди) |
| **#32** | [Архітектурна діаграма проекту](#32-архітектурна-діаграма-проекту) |
| **#33** | [Демо-відео (30-60 секунд)](#33-демо-відео-30-60-секунд) |
| **#34** | [Метрики для README (портфоліо)](#34-метрики-для-readme-портфоліо) |
| **#35** | [Parent-Child Chunking для law_chunks](#35-parent-child-chunking-для-law_chunks) |
| **#36** | [valid_from / valid_to в метаданих law_chunks](#36-valid_from-valid_to-в-метаданих-law_chunks) |
| **#37** | [Reranker після hybrid search](#37-reranker-після-hybrid-search) |
| **#38** | [Faithfulness Gate — валідатор галюцинацій](#38-faithfulness-gate-валідатор-галюцинацій) |
| **#39** | [Валідатор істотних умов (Ukrainian Law Checklist)](#39-валідатор-істотних-умов-ukrainian-law-checklist) |
| **#40** | [MCP-сервери для української екосистеми](#40-mcp-сервери-для-української-екосистеми) |
| **#41** | [`needs_law_review` (boolean) дублює нову колонку `status`](#41-needs_law_review-boolean-дублює-нову-колонку-status) |
| **#42** | [`law_deps` лишається в `watched_laws` JSONB (без нормалізованої таблиці)](#42-law_deps-лишається-в-watched_laws-jsonb-без-нормалізованої-таблиці) |
| **#43** | [Read-path kill-switch у боті неповний (меню частково захардкоджене)](#43-read-path-kill-switch-у-боті-неповний-меню-частково-захардкоджене) |
| **#44** | [RLS policies](#44-rls-policies) |
| **#45** | [Написати Claude Code skill для автооновлення changelog](#45-написати-claude-code-skill-для-автооновлення-changelog) |
| **#46** | [Реєстр законів = файл; нормалізована таблиця `laws` (v2)](#46-реєстр-законів--файл-нормалізована-таблиця-laws-v2) |
| **#47** | [`law_change_log` RLS — blanket authenticated, без per-tenant scoping](#47-law_change_log-rls--blanket-authenticated-без-per-tenant-scoping) |

> **✅ ID-колізії розведено (session 14):** другі входження перенумеровано — «RLS policies» → **#44**, «Skill для changelog» → **#45**. Перші входження #12 («Admin Dashboard») і #20 («Service Builder процес») лишились без змін.
> **#1** відсутній історично (нумерація почалась з #2) — лишаємо як є; нові записи беруть наступний вільний номер.

---

## 🤖 AI та якість документів

> Це найважливіший розділ — якість документу = репутація продукту.

### 35. Parent-Child Chunking для law_chunks
- **Проблема:** Плоскі чанки по ~500 токенів руйнують структуру статей — посилання між абзацами втрачаються при пошуку.
- **Рішення:** Двохрівнева структура:
  - Мікро-чанк (абзац) → для точного пошуку
  - Батьківський чанк (вся стаття) → йде в промпт після знаходження мікро-чанку
- **Міграція:** Додати `parent_chunk_id uuid REFERENCES law_chunks(id)` в таблицю. При пошуку — повертати батька.
- **Пріоритет:** 🟡 важливо (покращує якість контексту для складних документів)

### 36. valid_from / valid_to в метаданих law_chunks
- **Критично для України** — закони змінюються дуже часто (особливо Податковий кодекс).
- **Міграція:**
  ```sql
  ALTER TABLE law_chunks ADD COLUMN valid_from date NOT NULL DEFAULT '2000-01-01';
  ALTER TABLE law_chunks ADD COLUMN valid_to   date;  -- NULL = актуальна зараз
  ```
- **Фільтр у пошуку:**
  ```javascript
  .lte('valid_from', today).or('valid_to.is.null,valid_to.gte.' + today)
  ```
- **Без цього:** ризик генерації документу за скасованою редакцією закону.
- **Пріоритет:** 🔴 критично (додати до наповнення RAG)

### 37. Reranker після hybrid search
- **Проблема:** hybrid_search повертає топ-5 за score, але score не враховує повний контекст запиту.
- **Рішення:** додати крок reranking в n8n після пошуку:
  ```
  hybrid_search → топ-20 → Cohere Rerank API → топ-5 найрелевантніших
  ```
- **Вартість:** ~$0.001 на запит. Cohere Rerank підтримує українську мову.
- **Пріоритет:** 🟡 важливо

### 38. Faithfulness Gate — валідатор галюцинацій
- **Ідея:** Субагент перевіряє кожен факт у згенерованому документі проти джерел з RAG. Якщо факт без підтвердження — блокує відправку або флагує юристу.
- **Поток:**
  ```
  Groq генерує документ
      ↓
  Faithfulness агент: "Чи є підтвердження кожного факту в context?"
      ↓ Pass                    ↓ Fail
  Відправляємо         Регенерація або флаг для ревью
  ```
- **Реалізація:** окрема n8n нода з промптом-ревьюером після генерації.
- **Пріоритет:** 🟡 важливо (особливо перед запуском реальних клієнтів)

### 39. Валідатор істотних умов (Ukrainian Law Checklist)
- **Ідея:** Кожен тип документа має обов'язкові умови за ЦК/ГК/СК України. Окремий агент перевіряє чи всі вони присутні у згенерованому документі.
- **Приклад для розлучення:**
  ```
  □ Дата та місце укладення шлюбу (ст. 34 СК)
  □ Спільне майно: перелік або відмова від поділу
  □ Діти: місце проживання та аліменти (якщо є)
  □ Підсудність: який суд (ст. 57 ЦПК)
  ```
- **Якщо щось пропущено** → агент доповнює або повертає на регенерацію.
- **Зберігати чек-листи** у Supabase як частину конфігурації послуги (`services.required_fields`).
- **Пріоритет:** 🔴 критично (це різниця між юридично коректним і просто красивим документом)

### 40. MCP-сервери для української екосистеми
- **ЄДР / Опендатабот:** валідація контрагентів — статус компанії, підписанти, КВЕДи, банкрутство.
- **ЄДРСР (реєстр судових рішень):** як суди трактують конкретні пункти — корисно для GraphRAG.
- **Дія.Підпис / Вчасно:** після генерації документу — відправка на підпис через MCP.
- **Пріоритет:** 🔵 стратегічно (після стабілізації генерації документів)


- ✅ Done: 2026-03-21
- n8n тепер читає `services.ai_prompt` і використовує як system prompt для Groq

### 2. RAG з українськими законами — парсинг + векторизація
- ✅ Done: 2026-03-21
- law_chunks таблиця з vector(768), gemini-embedding-001
- Hybrid search: vector + FTS, топ-5 статей → в промпт
- Засіяно: СК України, ЦПК України

### 2а. Моніторинг змін у законах — повна архітектура
- **DB schema (Крок 1):** ✅ Done 2026-03-27 — `007_watched_laws.sql` виконано в Supabase. watched_laws для divorce заповнені реальними датами редакцій.
- **CRON reference impl:** `scripts/check-law-updates.mjs` — референс для n8n workflow (сесія 8)
- **Проблема:** Закони на zakon.rada.gov.ua регулярно оновлюються. Якщо ст. 183 СК зміниться, а шаблон залишиться старим — клієнт отримає неправильний документ. Юридична відповідальність.
- **Джерело правди:** zakon.rada.gov.ua — на кожній сторінці є рядок _"Редакція від ДД.ММ.РРРР"_ — це дата останньої зміни.

**Крок 1 — Прив'язати послуги до законів (DB schema):**
```sql
-- Нова колонка в таблиці services:
ALTER TABLE services ADD COLUMN watched_laws JSONB DEFAULT '[]';

-- Структура watched_laws:
-- [
--   {
--     "slug": "sk-ukraine",
--     "title": "Сімейний кодекс України",
--     "url": "https://zakon.rada.gov.ua/laws/show/2947-14",
--     "last_known_date": "2024-03-15",   -- дата яку ми зберегли при додаванні
--     "articles": ["ст.105", "ст.110", "ст.183"]  -- конкретні статті що використовуємо
--   },
--   {
--     "slug": "cpk-ukraine",
--     "title": "ЦПК України",
--     "url": "https://zakon.rada.gov.ua/laws/show/1618-15",
--     "last_known_date": "2024-01-20",
--     "articles": ["ст.175"]
--   }
-- ]
```
Юрист заповнює `watched_laws` через Service Builder Checklist (розділ 4) при створенні послуги.

**Крок 2 — CRON workflow в n8n (раз на тиждень):**
```
Scheduled Trigger (Monday 09:00)
  → Get All Services (Supabase: SELECT id, title, watched_laws FROM services)
  → Loop Each Service
      → Loop Each Law in watched_laws
          → HTTP GET zakon.rada.gov.ua/{url}
          → Parse "Редакція від ДД.ММ.РРРР" (regex з HTML)
          → Compare parsed_date vs last_known_date
          → IF changed:
              → UPDATE services SET needs_law_review = true WHERE id = ...
              → UPDATE watched_laws[i].last_known_date = parsed_date
              → Collect changed_services[]
  → IF changed_services not empty:
      → Send Telegram alert to admin:
        "⚠️ Закони змінились! Перевірте послуги:
         • Розлучення (СК України від 15.03.2024 → 02.04.2025)
         • Аліменти (СК України)"
```

**Крок 3 — Відображення в адмін-панелі:**
```sql
ALTER TABLE services ADD COLUMN needs_law_review BOOLEAN DEFAULT false;
```
- Картка послуги: жовтий банер `"⚠️ Закон змінився — перевірте шаблон"` якщо `needs_law_review = true`
- Після перевірки юрист натискає "Перевірено" → `needs_law_review = false`

**Крок 4 — Хто отримує сповіщення (окрема задача 🔖):**
> Потрібно визначити: адмін платформи? юрист-автор послуги? всі юристи на яких є підписники?
> Зараз: тільки адмін в Telegram. Далі — через таблицю `lawyers` + їх `telegram_id`.

- **Пріоритет:** 🟡 важливо (юридична відповідальність)
- **Файли:** `supabase/migrations/`, `n8n-workflows/law-monitor-weekly.json`, `src/admin/pages/ServiceEditPage.tsx`
- **Актуальність:** Після додавання 2-3 послуг — інакше немає сенсу моніторити 1 закон

### 2б. Агент-критик після генерації документу
- **Зараз:** Документ генерується і одразу відправляється клієнту без перевірки
- **Як краще:** Другий LLM-виклик (Groq) після генерації:
  ```
  "Перевір документ: 1) Чи є вигадані статті? 2) Чи всі дані клієнта використані?
   3) Чи правильна структура? Відповідь: JSON {ok: bool, issues: string[]}"
  ```
  Якщо `ok: false` → регенерація (макс 2 спроби) → якщо знову fail → alert адміну
- **Пріоритет:** 🟡 важливо
- **Актуальність:** Після підключення `ai_prompt` і RAG

### 3. Структура Google Doc — базовий шаблон
- ✅ Done: 2026-03-21 (ai_prompt), переведено на JS шаблон 2026-03-23
- `n8n-templates/divorce-document.js` — детермінований шаблон, 0% галюцинацій для даних

### 4. Мінімізація галюцинацій
- ✅ Done: 2026-03-23 (hybrid template approach)
- 95% документа генерується JS шаблоном (детерміновано, без AI)
- AI використовується тільки для відмінювання ПІБ, назв установ та дітей
- Статті законів захардкоджені в шаблоні — неможливо вигадати

### 4a. Старі посилання на форму в Telegram залишаються клікабельними
- **Зараз:** Після оновлення form_config користувач може натиснути на старе повідомлення бота з посиланням → відкриє форму з новим конфігом, але контекст (service_id, chat_id) може бути неактуальним
- **Як краще:** Або додати версіонування форм (form_config_version), або перевіряти актуальність при відкритті TWA
- **Пріоритет:** 🟡 важливо
- **Актуальність:** Поки форма одна — некритично, але з масштабуванням стане проблемою

### 4b. Якість AI генерації — виявлені проблеми
- ✅ Fixed: 2026-03-23 (hybrid template approach)
- Всі проблеми вирішені переходом на JS шаблон:
  1. ✅ Відмінки — окремий AI-запит тільки для ПІБ/установ/дітей
  2. ✅ Дати — конвертуються в JS (`formatDate`), неможливо переплутати
  3. ✅ Тип провадження — умовна логіка в JS, не в промпті
  4. ✅ Судові витрати — `court_costs_on` перевіряється в JS
  5. ✅ Офіційна електронна адреса — завжди виводиться
  6. ✅ Done 2026-03-23: DROP COLUMN `system_prompt` — міграція `004_drop_system_prompt.sql` виконана

### 4с. Текст спільного майна — неінформативний бланк
- **Зараз:** `"За час шлюбу сторонами набуто спільне майно: ________."`  — порожній бланк
- **Як краще:** Якщо `property_dispute: 'separate'` → прибрати бланк, залишити тільки:
  `"За час шлюбу сторонами набуто спільне майно. Спір щодо поділу буде вирішуватися в окремому провадженні."`
- **Файл:** `n8n-templates/divorce-document.js`
- **Пріоритет:** 🟡 важливо (документ виглядає незавершеним)

---

## 💳 Монетизація та партнери

### 5. Відсутні `lawyer_type` і `revenue_share_percent`
- **Зараз:** Всі юристи рівні, немає поділу "партнер" vs "інфлюенсер"
- **Як краще:**
  ```sql
  ALTER TABLE lawyers ADD COLUMN lawyer_type text DEFAULT 'partner'; -- 'partner' | 'influencer'
  ALTER TABLE lawyers ADD COLUMN revenue_share_percent int DEFAULT 0; -- 0–100
  ```
- **Пріоритет:** 🟡 важливо
- **Актуальність:** Потрібно до підключення першого інфлюенсера

### 6. Таблиця `transactions` відсутня
- **Зараз:** Немає трекінгу платежів — неможливо знати скільки кому виплатити
- **Як краще:**
  ```sql
  CREATE TABLE transactions (
    id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
    case_id uuid REFERENCES cases(id),
    service_id int REFERENCES services(id),
    amount numeric NOT NULL,
    platform_cut numeric,
    lawyer_cut numeric,
    status text DEFAULT 'pending', -- 'pending' | 'paid'
    created_at timestamptz DEFAULT now()
  );
  ```
- **Пріоритет:** 🟢 nice-to-have (поки платежів немає)
- **Актуальність:** Потрібно коли з'являться реальні транзакції

### 7. При реєстрації юриста не створюється запис у `lawyers`
- **Зараз:** `auth.users` запис створюється, але `lawyers` таблиця залишається порожньою
- **Як краще:** В `LoginPage.tsx` після `signUp()` → INSERT в `lawyers` з `id = user.id`
- **Пріоритет:** 🔴 критично (для коректної роботи revenue share)
- **Актуальність:** Блокує монетизацію

---

## 🛠 Адмін-панель

### 8. Помилка збереження — тихий fail
- **Зараз:** Якщо Supabase повертає помилку при save → `setSaving(false)` але юрист не знає
- **Як краще:** Toast-повідомлення "Помилка збереження: [текст помилки]" червоним кольором
- **Пріоритет:** 🟡 важливо
- **Актуальність:** Треба до показу продукту партнерам

### 9. Quality checklist в AI-промпт вкладці — тільки візуальний
- **Зараз:** Чекбокси "Вказав суд / Навів статті / Без галюцинацій" — не зберігаються
- **Як краще:** Або прибрати, або зберігати в `services.ai_quality_flags` JSONB
- **Пріоритет:** 🟢 nice-to-have
- **Актуальність:** Не блокує нічого

### 10а. DatePickerField — навігація по роках незручна
- **Зараз:** Вибір дати далекого року (напр. 1985) вимагає багато кліків через сторінки по 12 років
- **Як краще:** Додати поле ручного введення дати у форматі ДД.ММ.РРРР поруч з кнопкою календаря — для швидкого вводу без навігації
- **Альтернатива:** При кліку на рік — показувати input з числом замість grid
- **Пріоритет:** 🟡 важливо (UX — багато клієнтів вводять дату народження 30+ років назад)
- **Файл:** `apps/client/src/components/form/fields/DatePickerField.tsx`

### 10. Drag & drop полів у конструкторі
- **Зараз:** Кнопки ↑↓ для переміщення полів
- **Як краще:** `@dnd-kit/core` для drag & drop
- **Пріоритет:** 🟢 nice-to-have
- **Актуальність:** UX комфортний навіть зараз для невеликих форм

### 11. Form preview — ручний refresh
- ✅ Done: 2026-03-25 (session 4.5) — live preview без refresh кнопки, auto-update при змінах config
- Залишилась кнопка "🔄 Скинути" яка скидає тільки відповіді юзера (не перемонтує компонент)

### 11а. Адмін-панель — UX для юриста
- ✅ Done: 2026-03-25 (session 4.5)
- **Auto-slug**: назва послуги → автоматичний `service_id` через транслітерацію (UA→Latin). Кнопка-олівець для ручного редагування
- **Auto-field-ID**: label поля → автогенерація ID якщо починається з `field_`. Прихований у "Технічні деталі" (collapsed за замовчуванням)
- **Clone field** (📋): клонує поле з новим унікальним ID, вставляє під оригінал
- **Move to tab**: дропдаун у FieldEditor — переміщує поле між табами
- **show_if оператор**: дропдаун `==` (дорівнює) / `!=` (не дорівнює) замість захардкодженого `==`
- **Onboarding hints**: порожній стан форми показує інструкцію "Як створити форму: 1. 2. 3."
- **Unsaved changes**: індикатор "● Незбережено" + `beforeunload` попередження при закритті вкладки
- **Responsive**: mobile sidebar drawer (гамбургер), compact top bar, короткі назви табів на мобілці ("Форма / AI / Опції"), preview modal (кнопка 👁 на < xl екранах)
- **Custom scrollbars**: 5px, slate-700 колір, `scrollbar-width: thin` для Firefox
- **Forgot/Reset password**: сторінки `ForgotPasswordPage` + `ResetPasswordPage`, `PASSWORD_RECOVERY` event обробляється глобально в `AdminApp.tsx`
- **Invite-only**: Supabase "Enable sign ups" вимкнено — нових юристів додає адмін вручну через Supabase Dashboard

### 11б. Парсинг законів при створенні послуги — чому це важливо
- **Проблема:** Юрист несе юридичну відповідальність за актуальність документу. Якщо стаття закону змінилась, а шаблон — ні, клієнт отримує неправильний документ. Це репутаційні та юридичні ризики.
- **Концепція "закон per послуга":**
  При створенні кожної нової послуги юрист вводить:
  1. **Які закони використовуються** (URL на zakon.rada.gov.ua + конкретні статті)
  2. **Чому кожен закон важливий** для цієї послуги (опис в чеклисті)
  Система парсить сторінку закону, зберігає поточну дату редакції (`last_known_date`), і далі моніторить зміни.
- **Чому юристу треба пояснювати:** Більшість юристів вважають "закон є закон — він не змінюється". Але на zakon.rada.gov.ua видно рядок _"Редакція від ДД.ММ.РРРР"_ — СК України змінювався понад 80 разів. Потрібно показати юристу конкретний приклад.
- **Що ми будуємо послугу за послугою:**
  - `divorce` → [СК ст.105, 110, 183, ЦПК ст.175]
  - `alimony` → [СК ст.180, 181, 182, 232]
  - `military_discharge` → [Закон про військову службу, ...]
  З кожною послугою база `watched_laws` зростає, CRON-моніторинг має більший сенс.
- **Детальна архітектура:** Дивись п.2а вище (CRON flow, DB schema, Telegram alert)
- **Пріоритет:** 🟡 важливо — додавати `watched_laws` при кожній новій послузі, навіть якщо CRON ще не написаний
- **Що зробити на наступній зустрічі з юристом:** попросити назвати 2-3 статті які він використовує → вписати в `watched_laws` → запустити парсер вручну для верифікації

### 12. Повноцінний Admin Dashboard (статистика + управління юристами)
- **Зараз:** Адмін-панель = Service Builder для одного юриста (власний обліковий запис)
- **Проблема:** Немає центрального місця для власника платформи — бачити всіх юристів, їх послуги, метрики

**Що потрібно (майбутнє):**
```
Власник платформи (super-admin):
├── Dashboard — активні юристи, кількість замовлень, виручка, конверсія
├── Юристи — список, статус, запрошення (ТІЛЬКИ за invite-link)
│   └── Invite flow: super-admin генерує токен → юрист реєструється за посиланням
│       → автоматично отримує роль 'lawyer', прив'язується до lawyer_id
├── Послуги — всі послуги всіх юристів, фільтр/пошук
├── Замовлення — всі кейси, статус, час генерації, якість
└── Моніторинг — помилки n8n, AI-виклики, ліміти Groq/Google

Юрист (поточна роль):
└── Service Builder — тільки свої послуги (вже є ✅)
```

**DB зміни:**
```sql
-- Ролі
ALTER TABLE auth.users ADD COLUMN role TEXT DEFAULT 'lawyer'; -- 'super_admin' | 'lawyer'

-- Запрошення
CREATE TABLE invites (
  token UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  created_by UUID REFERENCES auth.users,
  used_by UUID REFERENCES auth.users,
  used_at TIMESTAMPTZ,
  expires_at TIMESTAMPTZ DEFAULT NOW() + INTERVAL '7 days'
);
```

**Пріоритет:** 🔴 відкласти — спочатку якість і кілька юристів вручну
**Актуальність:** Актуально коли юристів > 3-5, або при залученні інвестицій
**Поточний підхід:** Юристи додаються вручну через Supabase Auth dashboard — достатньо для MVP

---

## 🏗 Інфраструктура та масштабування

### 31. Security plugin для всієї команди
- **Ідея:** Коли з'явиться команда — додати `security-guidance` як project-level хук в `.claude/settings.json`, щоб захист працював у всіх без ручної установки плагіну.
- **Що робить плагін:** 3 шари захисту: regex (~25 паттернів: XSS, ін'єкції, hardcoded secrets, eval, pickle...) → LLM-ревью дифу → агентський ревью при `git commit`.
- **Офіційний плагін:** `github.com/anthropics/claude-code/plugins/security-guidance`
- **Зараз:** кожен розробник ставить `/plugin install security-guidance` локально у своєму Claude Code.
- **Потім (командний варіант):** project-level хук коммітиться в репо → працює у всіх автоматично.
- **Пріоритет:** 🔵 стратегічно (коли буде команда 2+ людини)

### 20. Service Builder — процес додавання нової послуги
- **Зараз:** Нова послуга = ручна робота: розробник сам пише form_config, шаблон документу, тести
- **Мета:** Юрист заповнює чеклист → розробник по ньому будує послугу за 1 сесію
- **Документ:** `docs/service-builder-checklist.md` — шаблон для зустрічі з юристом

**Чеклист складається з:**
1. **Питання до клієнта** → `form_config` (поля, таби, show_if умови, підказки)
2. **Шаблон документу з плейсхолдерами** → `{service}-document.js` (JS builder) або Google Docs template
3. **Нормативна база** → хардкод статей + `watched_laws` для моніторингу
4. **Сценарії** → `test-data/{service}/fixtures/` (2-4 кейси)

**Flow:**
```
Зустріч з юристом (1-2 год)
  → Юрист заповнює docs/service-builder-checklist.md
  → Розробник будує послугу (1 сесія = Session 4)
  → Тест end-to-end з реальними даними
  → Юрист перевіряє якість документу
  → Публікація в боті
```
- **Пріоритет:** 🟡 важливо (масштабування)
- **Актуальність:** Підготовлено до зустрічі з юристом

### 15. Перейменування проекту `legal-twa` → `legal-ai`
- **Зараз:** Назва `legal-twa` відображає тільки Telegram Web App — один канал
- **Як краще:** При додаванні веб-сайту або мобільного застосунку перейменувати репозиторій і Vercel project на `legal-ai` або `legalai-platform`
- **Пріоритет:** 🟢 nice-to-have
- **Актуальність:** Не критично поки є тільки Telegram канал

---

## ⚡ Технічний борг

### 44. RLS policies
- ✅ Done: 2026-03-23 — міграція `005_enable_rls.sql` виконана
- `cases`, `identities` — заблоковані для anon (тільки service_role)
- `law_chunks`, `law_documents`, `services`, `courts`, `lawyers` — SELECT публічний, запис заблокований
- **Залишилось (після впровадження Supabase Auth):**
  ```sql
  CREATE POLICY "users_own_cases" ON cases
    FOR ALL USING (user_id = auth.uid());
  CREATE POLICY "lawyers_own_services" ON services
    FOR ALL USING (lawyer_id = auth.uid() OR lawyer_id IS NULL);
  ```

### 16. GDPR / Захист персональних даних
- **Шифрування:** ✅ Done 2026-03-27 — AES-256-GCM в n8n Code Node ("Encrypt Data")
  - Формат: `v1:<iv>:<authTag>:<ciphertext>` (версіонований для ротації ключів)
  - Ключ в n8n Global Config, дані в Supabase → компрометація одного не розкриває інше
  - Міграція 008: `encrypted_data` JSONB → TEXT
  - Утиліта: `scripts/decrypt-case.mjs` для GDPR-запитів на доступ до даних
- **Consent:** ✅ Done 2026-03-27 — екран-дисклеймер перед формою
  - Показується при відкритті TWA: опис процесу, захист даних, строк зберігання
  - `consented_at` timestamp зберігається в `cases` таблиці
  - Модалка "Політика конфіденційності" з 6 розділами (дані, використання, захист, строк, права, контакти)
- **Retention:** ✅ Done 2026-03-27 — міграція 009
  - `delete_expired_cases()` SQL-функція (service_role only)
  - `expires_at` = 90 днів (було 30)
  - Потрібно: n8n Scheduled Trigger для щотижневого виклику
- **Privacy Policy:** ✅ Done 2026-03-27 — `PrivacyPolicyModal.tsx`
- **Що залишилось:**
  1. ⬜ **Self-service видалення** — команда в Telegram боті (напр. /delete_my_data або кнопка "Видалити мої дані") → n8n workflow видаляє кейси по user_id. Зараз: запит через бота обробляється вручну.
  2. ⬜ **Self-service продовження** — можливість запросити НЕ видаляти дані (обнулити expires_at). Зараз: через бота вручну.
  3. ⬜ **n8n CRON** — Scheduled Trigger для щотижневого виклику `delete_expired_cases()`
  4. ⬜ **Data minimization** — не зберігати поля які не потрібні для документа
- **Пріоритет:** 🟢 nice-to-have (основне GDPR зроблено)
- **Актуальність:** Self-service видалення — до масштабування (>100 юзерів)

### 13а. Global Config нода — секрети в коді замість n8n Variables
- **Зараз:** `SUPABASE_SERVICE_KEY` і `ENCRYPTION_KEY` захардкоджені прямо в JS-коді ноди Global Config
- **Чому так:** Обхідний шлях для n8n Cloud Starter plan (Variables там платні)
- **Як краще:** Self-hosted n8n (локально або VPS) має Variables безкоштовно:
  1. n8n Settings → Variables → додати `SUPABASE_SERVICE_KEY`, `ENCRYPTION_KEY`
  2. В Global Config ноді: `$vars.SUPABASE_SERVICE_KEY` замість хардкоду
  3. Секрети більше не в коді — безпечніше, легше ротувати
- **Пріоритет:** 🟡 важливо (безпека)
- **Актуальність:** Зробити при переносі на VPS — тоді налаштовуємо Variables один раз

### 13. `supabase-migrations.sql` — ручне виконання
- **Зараз:** Треба відкривати Supabase SQL Editor і копіювати вручну
- **Як краще:** Supabase CLI + `supabase/migrations/` директорія для version control
- **Пріоритет:** 🟢 nice-to-have
- **Актуальність:** Не критично поки проект маленький

### 14. `vercel.json` rewrites — потенційний конфлікт
- **Зараз:** `"source": "/(.*)"` перехоплює ВСЕ включно з майбутніми `/api/*` routes
- **Як краще:** Уточнити до `"source": "/(?!api/)(.*)"`  коли з'являться serverless functions
- **Пріоритет:** 🟢 nice-to-have
- **Актуальність:** Не блокує поки немає API routes

### Тимчасові компроміси фічі `service-lifecycle` (spec 2026-06-08)

> Свідомо прийняті при плануванні `service-lifecycle` (backend-фундамент). Зафіксовано, щоб не загубити. Спека: `specs/features/service-lifecycle/`.

### 41. `needs_law_review` (boolean) дублює нову колонку `status`
- **Зараз:** наявний `services.needs_law_review BOOLEAN` (міграція 007) і нова `services.status` (`active|needs_review|disabled`) описують частково те саме. `status` зроблено авторитетним джерелом правди для «чи обслуговувати послугу», а boolean лишено недоторканим — два сигнали замість одного.
- **Чому компроміс:** прибрати boolean одразу = більший scope + ризик зачепити наявні читання. Відклали, щоб фіча лишалась тонкою.
- **Як краще:** після того як ніщо не читає `needs_law_review` — окремою міграцією видалити колонку (або перетворити на згенерований `status='needs_review'`).
- **Пріоритет:** 🟢 nice-to-have (прибрати drift двох джерел правди)
- **Актуальність:** до того, як зʼявиться automated law-monitoring чи admin-UI ревʼю.

### 42. `law_deps` лишається в `watched_laws` JSONB (без нормалізованої таблиці)
- **Зараз:** звʼязок «закон → послуги» виводиться запитом по `services.watched_laws` (JSONB), окремої таблиці `law_deps`/`law_relations` немає.
- **Чому компроміс:** нормалізована таблиця = дублювання даних + синхронізація; на 2-3 послугах JSONB-запит достатній. Узгоджено з master-context («law_deps = розширення watched_laws»).
- **Як краще:** проміжний крок вже зроблено (G4) — канонічний реєстр-файл `scripts/law-registry.mjs` (ідентичність закону по URL) + `validate`/`normalize` тримають `watched_laws` у каноні. Далі при GraphRAG (#23) — нормалізована `law_relations`/`laws` з HITL-наповненням; тоді ж мігрувати зворотний індекс (див. #46).
- **Пріоритет:** 🔵 стратегічно (разом з #23 GraphRAG)
- **Актуальність:** коли послуг стане багато або JOIN-запити «закон→послуги» почнуть гальмувати.

### 43. Read-path kill-switch у боті неповний (меню частково захардкоджене)
- **Зараз:** меню послуг у `main-bot.json` будується Switch-нодою (захардкоджені пункти). Kill-switch на read-path лише НЕ віддає кнопку TWA для неактивної послуги; динамічного каталогу за `status` немає. Авторитетний guard — на write-path (`form-submit`), тож генерація все одно блокується.
- **Чому компроміс:** динамічний каталог = переробка бот-меню, поза scope фічі. Безпека збережена (генерацію блокує сервер).
- **Як краще:** будувати меню бота з `services WHERE status='active'` (динамічний каталог) — природно лягає в майбутній Service Builder (#18/#20).
- **Пріоритет:** 🟡 важливо (UX: користувач не має бачити пункт, який потім скаже «недоступно»)
- **Актуальність:** до публічного запуску з 3+ послугами.

### 46. Реєстр законів = файл; нормалізована таблиця `laws` (v2)
- **Зараз (G4):** канонічна ідентичність закону живе у файлі `scripts/law-registry.mjs` (один `{slug,title,url}` на закон). Зв'язок «закон→послуги» — у `services.watched_laws` JSONB; зіставлення по **URL** (не по slug). `validate` ловить дрейф, `normalize` приводить до канону.
- **Чому компроміс:** n8n у рантаймі `watched_laws` не читає (статті вшиті в JS-шаблон), тож файла достатньо як SSoT для ідентичності + валідації. Повноцінна таблиця = міграція + бекфіл + синхронізація без реальної потреби зараз.
- **Як краще:** у v2/GraphRAG (#23, #42) винести закони в таблицю `laws` (PK = rada-id/URL), а `watched_laws` → FK-зв'язки; реєстр-файл стане сідом міграції. Заодно прибрати ризик розсинхрону файл↔БД.
- **Ризик зараз:** новий закон, доданий у `watched_laws` повз реєстр, не матчиться — мітигується командою `validate` (запускати після додавання послуги/закону).
- **Пріоритет:** 🔵 стратегічно (разом з #23/#42)
- **Актуальність:** коли зʼявляться вузли законів GraphRAG або багато послуг ділять закони.

### 47. `law_change_log` RLS — blanket authenticated, без per-tenant scoping
- **Зараз (migration 013):** панель ревʼю читає/оновлює `law_change_log` через політики `SELECT`+`UPDATE` для ролі `authenticated` (`USING (true)`). Будь-який залогінений юрист бачить усі рядки й може писати в будь-яку колонку рядка. `INSERT`/`DELETE` лишаються service_role-only (append-only з UI).
- **Чому компроміс:** зміни законів = глобальні законодавчі факти (СК/ЦПК зачіпають усіх), не per-tenant дані; юрист зараз фактично один (Ольга). Scoping/обмеження колонок = більший scope без реальної потреби.
- **Як краще:** (а) коли стане кілька юристів і журнал треба ділити — додати tenant-фільтр (напр. через `affected_services` → `services.lawyer_id`) або gate на `admin_users`/JWT-claim замість голого `authenticated`; (б) щоб юрист не міг переписати будь-яку колонку чи підмінити `reviewed_by` — замінити пряму `UPDATE`-політику на security-definer RPC `review_law_change(log_id, action, notes)`, що сам штампує `reviewed_by = auth.email()` + `reviewed_at = now()` і ігнорує решту колонок (або BEFORE UPDATE-тригер, що перезаписує ці поля з JWT).
- **Security-review (2026-06-11) — підтверджено, не блокує:** автоматичний скан позначив 3 знахідки на migration 013 (broad `USING(true)` UPDATE; bare-`authenticated` gate; client-stamped `reviewed_by` у `LawChangeLogPage.tsx`). **Головна мітигація:** публічна реєстрація вимкнена (`auth.settings.disable_signup=true`, invite-only) → роль `authenticated` = лише запрошена команда (зараз **1** акаунт). Тож «будь-який authenticated» = довірена особа, а не інтернет. Спуфінг `reviewed_by` у цій моделі — low-risk audit-nit. Усе вище («Як краще») — план хардингу до пункту-тригера нижче.
- **Пріоритет:** 🟢 nice-to-have, поки signup вимкнено + один юрист.
- **Актуальність (тригер хардингу):** ПЕРЕД увімкненням self-signup АБО онбордингом 2-го юриста — тоді обовʼязково RPC/тригер для штампу + role-gate (не голий `authenticated`).

---

## 📐 Архітектура шаблонів та Service Builder

### 17. Шаблони документів: від JS builder до Google Docs плейсхолдерів
- **Зараз:** `divorce-document.js` генерує plain text → вставляється через один плейсхолдер `{{DOCUMENT_CONTENT}}` в Google Docs
- **Проблема:** Юрист не може редагувати шаблон (потрібно правити JS-код). Форматування базове.
- **Три підходи:**
  - **A) JS builder** (поточний) — для складних документів з умовною логікою (6+ if/else блоків). Повний контроль, тестується локально, 0% галюцінацій.
  - **B) Google Docs template з плейсхолдерами** — для простих документів. Юрист створює красивий шаблон з `{{PLAINTIFF_NAME}}`, `{{MARRIAGE_DATE}}` тощо → n8n робить batch replace. Юрист контролює форматування.
  - **C) Гібрид** — JS builder генерує не текст, а **Map плейсхолдерів** → заповнюється красивий Google Docs шаблон. Складна логіка в JS, красивий результат у Docs.
- **Рекомендація:** Підхід C для нових послуг. JS builder генерує секції, Google Docs шаблон визначає розташування і форматування. Юрист редагує шаблон у Google Docs, розробник — логіку в JS.
- **Пріоритет:** 🟡 важливо (блокує Service Builder)
- **Актуальність:** Потрібно до підключення другої послуги

### 18. Service Builder — конструктор послуг для юристів
- **Ідея:** Юрист самостійно додає нову послугу без розробника:
  1. Створює Google Docs шаблон з плейсхолдерами
  2. Налаштовує форму в адмін-панелі (поля, таби, умови)
  3. Система автоматично маппить плейсхолдери на поля форми
  4. n8n workflow заповнює шаблон і доставляє клієнту
- **Необхідні кроки:**
  1. Адмін-панель: конструктор форм (вже є базово)
  2. Google Docs API: парсинг плейсхолдерів з шаблону
  3. UI маппінгу: плейсхолдер ↔ поле форми
  4. n8n generic workflow: роутинг по service_slug → відповідний шаблон
- **Пріоритет:** 🟡 важливо (ключова фіча для масштабування)
- **Актуальність:** Після стабілізації 2-3 послуг вручну

### 19. Архітектурна оцінка (станом на 2026-03-24)
- ✅ **Form engine** — сервіс-агностичний, будь-який JSON config
- ✅ **Stale answer cleanup** — автоматично очищає приховані поля
- ✅ **Test infrastructure** — fixtures + golden files + assertions
- ✅ **RLS** — базовий захист таблиць
- 🟡 **Document generation** — прив'язана до конкретного сервісу (один JS файл на послугу)
- 🟡 **Delivery pipeline** — n8n, немає error recovery / retry
- ✅ **PII encryption** — AES-256-GCM в n8n, формат `v1:<iv>:<tag>:<ct>` (done 2026-03-27)

---

## 🧠 GraphRAG та AI-асистент форми

### 26. Голосове заповнення форми через Telegram
- **Ідея:** Користувач у стресі — говорити простіше ніж друкувати 30+ полів. Відправляє голосове повідомлення боту → Whisper транскрибує → AI заповнює поля форми.
- **Поток:**
  ```
  Telegram voice message → n8n отримує audio_file_id
  → Whisper API ($0.006/хв) → текст
  → AI (той самий асистент форми з #25) → agent.setState(fields)
  → Користувач бачить заповнені поля → підтверджує
  ```
- **Чому Telegram, не Web Speech API:** органічно для нашої аудиторії, працює на iOS, якість вища.
- **Комбінується з:** #25 (AI-асистент форми) — голос це просто ще один спосіб дати контекст асистенту.
- **Вартість:** ~$0.006 за хвилину Whisper API, середня форма = 2-3 хв = ~$0.02 на кейс.
- **Пріоритет:** 🟡 важливо (після реалізації #25 AI-асистента)

### 23. GraphRAG — граф зв'язків між законами (поетапно)
- **Ідея:** Складні документи (розлучення + аліменти + поділ майна) вимагають розуміння зв'язків між законами, а не просто пошуку схожого тексту. Граф дозволяє агенту traversal-запитами знаходити ланцюжки: Ст. 109 СК → вимагає → Ст. 57 ЦПК → уточнюється → Постанова Пленуму №11.
- **Етап 1 (Supabase):** Додати таблицю `law_relations (from_chunk_id, to_chunk_id, relation_type)` — без нової інфраструктури, валідуємо концепцію.
- **Етап 2 (Neo4j):** Мігрувати якщо `law_relations` > 1000 записів або Supabase JOIN-запити стають громіздкими.
- **Наповнення:** AI витягує зв'язки автоматично → Ольга (юрист) перевіряє через інтерфейс HITL.
- **Пріоритет:** 🔵 стратегічно (після стабілізації 2-3 послуг)

### 24. Інтерфейс для Ольги (HITL — Human-in-the-Loop)
- **Ідея:** Юрист верифікує зв'язки які AI витягнув із законів. Жоден факт не потрапляє в граф без перевірки.
- **Поток:**
  ```
  AI читає закон → витягує зв'язки → pending_review
  Ольга бачить картку: "Ст. 109 СК → вимагає → Ст. 57 ЦПК. Джерело: [фрагмент]"
  → Підтвердити / Відхилити / Виправити → law_relations
  ```
- **Технологія:** CopilotKit Shared State (L6 з курсу Generative UI) — Ольга і AI в одному потоці, без окремої сторінки рецензування.
- **Frontend Tool:** `highlightInDocument` — AI підсвічує фрагмент тексту з якого витягнув зв'язок.
- **Пріоритет:** 🔵 стратегічно (після Етапу 1 пункту #23)

### 25. AI-асистент для заповнення форми (Shared State)
- **Ідея:** Замість того щоб заповнювати 20+ полів вручну — користувач описує ситуацію в чаті, AI заповнює поля форми в реальному часі. Користувач перевіряє і підтверджує.
- **Приклад:**
  ```
  Користувач: "Ми одружені 7 років, двоє дітей 5 і 8 років, я хочу опіку"
  AI заповнює: years_married=7, children_count=2, children_ages=[5,8], custody="plaintiff"
  ```
- **Технологія:** CopilotKit Shared State — agent.setState() пише в state форми, React компоненти читають.
- **Frontend Tools:** navigateToSection (переключати таби), highlightField (підсвічувати поле).
- **Конкурентна перевага:** Жоден український юридичний сервіс так не робить. Люди в стресі — вони розповідають ситуацію, а не заповнюють анкету.
- **Пріоритет:** 🟡 важливо (після стабілізації поточних форм)

## 🛠️ SDD — Spec-Driven Development

### 45. Написати Claude Code skill для автооновлення changelog
- **Ідея:** Після завершення кожного фічера агент автоматично оновлює `apps/client/.claude/changelog.md` — як в курсі DeepLearning.AI "Spec-Driven Development".
- **Як:** Написати `.claude/skills/update-changelog.md` (глобально або локально в проекті), агент викликає його в кінці кожного feature loop.
- **Нині:** Changelog оновлюємо вручну в кінці сесії.
- **Пріоритет:** 🟡 важливо (покращує комунікацію зі стейкхолдерами і контекст між сесіями)

### 21. Створити Project Constitution для Legal-AI
- **Ідея:** Розбити `CLAUDE.md` на три окремі файли за SDD-шаблоном:
  - `specs/mission.md` — місія, аудиторія, scope
  - `specs/tech-stack.md` — React/Vite/TS + n8n + Supabase + Vercel, обмеження
  - `specs/roadmap.md` — фази: розлучення → аліменти → наступні документи
- **Навіщо:** Агент завантажує чіткий контекст на старті кожної сесії, немає drift між фічерами.
- **Пріоритет:** 🟡 важливо (фундамент для масштабування розробки)

### 22. Писати Feature Spec перед кожною новою формою документа
- **Ідея:** Для кожного нового юридичного документа (аліменти, поділ майна і т.д.) спочатку створювати:
  - `specs/features/<name>/plan.md`
  - `specs/features/<name>/requirements.md`
  - `specs/features/<name>/validation.md`
- **Навіщо:** Запобігає vibe coding, зберігає архітектурні рішення, зменшує cognitive debt.
- **Пріоритет:** 🟢 додати з наступної фічери

---

## ⚠️ AI Technical Debt — профілактика

> Чотири види боргу які накопичуються непомітно і ламають AI-продукти.

### 27. Prompt Debt — версіонування промптів
- **Проблема:** `services.ai_prompt` змінюється в Supabase без історії. Невідомо який промпт згенерував який документ.
- **Рішення:**
  - Додати `prompt_version` (text) в таблицю `services`
  - Зберігати всі версії промптів у `n8n/prompts/` з іменем файлу `<service>_v<N>.md`
  - При генерації записувати `prompt_version` у таблицю `cases`
- **Пріоритет:** 🟡 важливо

### 28. Model Debt — абстракція моделей
- **Проблема:** назва моделі `llama-3.3-70b-versatile` захардкожена в n8n workflow. Зміна моделі = ручне редагування всіх нод.
- **Рішення:** один Code node `00_config.js` на початку кожного workflow:
  ```javascript
  const MODELS = {
    generation: 'llama-3.3-70b-versatile',
    fallback: 'mixtral-8x7b-32768',
    embedding: 'gemini-embedding-001',
  }
  ```
  Всі інші ноди читають з `$('00_config').item.json.MODELS`.
- **Пріоритет:** 🟡 важливо

### 29. Retrieval Debt — актуальність RAG
- **Проблема:** закони в `law_chunks` можуть застаріти — документ генерується за старими нормами.
- **Що вже є:** `watched_laws` таблиця ✅, `scripts/check-law-updates.mjs` ✅
- **Чого не вистачає:**
  - Автоматичний запуск скрипту (CRON в n8n)
  - Алерт у Telegram адміну коли закон змінився
  - Позначка `is_outdated` у `law_chunks` до переіндексації
- **Пріоритет:** 🟡 важливо

### 30. Control Debt — моніторинг якості генерації
- **Проблема:** невідомо наскільки добре AI генерує документи. Немає сигналу про деградацію.
- **Рішення:**
  - Додати в таблицю `cases`: `quality_score smallint`, `quality_feedback text`, `generation_ms int`
  - Після доставки документу — питати користувача: "Документ виглядає правильним? 👍 / 👎"
  - Дашборд в адмін-панелі: середній score по послузі, час генерації, відсоток 👎
- **Пріоритет:** 🟡 важливо (додати разом із першою реальною послугою)

---

## 📊 Портфоліо та документація

### 32. Архітектурна діаграма проекту
- **Ідея:** Візуальна схема data flow для README і демо — менеджери з найму повинні зрозуміти систему за 10 секунд.
- **Що показати:**
  ```
  Telegram Bot → Mini App (React/Vite)
      ↓ submit
  n8n Webhook → Supabase (зберегти) → Groq (генерація)
      ↓ RAG                              ↓
  law_chunks (pgvector)          Google Docs API
      ↓ hybrid search                    ↓
  топ-5 статей → промпт          Telegram (доставка)
  ```
- **Формат:** Excalidraw або draw.io → PNG в `docs/assets/architecture.png`
- **Пріоритет:** 🟡 важливо (перед пошуком роботи)

### 33. Демо-відео (30-60 секунд)
- **Ідея:** Показати повний цикл: відкрив бота → заповнив форму → отримав документ у Telegram.
- **Структура:**
  1. Відкриваємо Telegram бот (5 сек)
  2. Заповнюємо форму розлучення (20 сек, прискорено)
  3. Отримуємо Google Doc посилання (5 сек)
  4. Відкриваємо документ — він готовий (10 сек)
- **Де розмістити:** README.md + LinkedIn пост
- **Пріоритет:** 🟡 важливо (перед пошуком роботи)

### 34. Метрики для README (портфоліо)
- **Ідея:** Замість "використовував n8n, Supabase, Groq" — показувати конкретний бізнес-результат.
- **Цільові метрики:**
  - ⏱ Час генерації документу: **< 30 секунд** (vs 2 години у юриста вручну)
  - 📚 Статей законів у RAG: **X чанків** з СК + ЦПК України
  - 📋 Кількість послуг: **N** (розлучення, аліменти, ...)
  - 👥 Кількість полів у найскладнішій формі: **30+**
  - ✅ Accuracy: **X%** 👍 від користувачів (після додавання quality_score)
- **Де взяти дані:** Supabase dashboard + cases таблиця після запуску
- **Формат у README:** значки shields.io або просто жирний текст
- **Пріоритет:** 🟡 важливо (зібрати після перших реальних кейсів)

---

## 📋 Як користуватись цим файлом

1. Коли кажемо "достатньо для MVP" — одразу додаємо запис сюди
2. Перед кожною сесією розробки — переглядаємо 🔴 пункти
3. Виконаний пункт — не видаляємо, а додаємо `✅ Done: [дата]`
4. Нові ідеї з розмов — теж сюди, щоб не загубились
