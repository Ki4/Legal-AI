# Legal AI — Skills & Harness: план дій

> Практичний план: що оформити як **Agent Skill**, що — як **ops-автоматизацію**,
> і як має виглядати наш **harness** (вся обвязка навколо моделі).
> Йди по чеклісту згори вниз. Посилається на ID з `IMPROVEMENTS.md` — не дублює їх.

---

## 🧭 Простими словами: Skills, Harness і де вони в нас

- **RAG** — *що* модель знає (дістає факти з бази у контекст). У нас — тільки для секції «обґрунтування».
- **Skill** — *як* виконувати наш рутинний воркфлоу. Папка з `SKILL.md`, яку Claude Code підвантажує сам, коли задача підходить.
- **Harness** — *чи спрацює це взагалі*: інструменти + перевірки + памʼять + права + пісочниця + оркестрація. Skill — це **один компонент** harness, не заміна йому.

**Ключове:** Skills у нас — це **не для кінцевого користувача**, а для **нас самих**, коли ми
працюємо через Claude Code / підтримуємо систему. Ми один раз записуємо «як ми робимо X у цьому
репо» — і далі агент робить це сам. «Чистий ROI» = нова інфраструктура не потрібна (за Claude Code
вже платимо), продакшн не чіпаємо, ризик нульовий, економія часу — одразу.

**Що НЕ робимо:** не «агентифікуємо» генерацію документів для користувача. Детермінований
шаблон (`render-document.js`) — це і є правильний harness для юридично-критичної задачі.

---

## 📦 Частина 1. Що оформити як Skill (для Claude Code)

| # | Операція | Тип | Чому саме так | Бэклог |
|---|----------|-----|---------------|--------|
| **S1** | **Новий сервіс** (form_config + document_template + golden citations + міграція + parity-тести) | ✅ **Skill** (dev) | Головний bottleneck масштабування. Зараз кожна послуга — руками. | — |
| **S2** | **Синхронізація цитат** (`ст. X` з шаблону ↔ `watched_laws` ↔ `law-registry.mjs` → golden) | ✅ **Skill** (dev) | Вже є `scripts/extract-citations.mjs` — обгорнути у skill. | #36 |
| **S3** | **Індексація законів у RAG** (fetch zakon.rada → розбити по статтях → embedding → `replace_law_chunks()`) | ✅ **Skill** (dev/ops) | Підготовка до RAG, повторювана ручна робота. | #2 |
| **S4** | **Генерація form_config** з тексту вимог | ✅ **Skill** (dev) | ~50% boilerplate форм геть. | #25 |
| **S5** | **QA-валідатор документа** (поля заповнені, нема `____`, падежі, орфографія → `quality_score`) | 🟡 **Гібрид**: skill (зібрати правила) + harness-нода (виконувати в рантаймі) | Частина — у пайплайні, частина — dev-інструмент. | #30 |
| **M1** | **Кеш аналізу проекту** (skill дивиться git-хеш області → читає кеш або переаналізовує й перезаписує) | ✅ **Skill** (dev/memory) | Ми часто аналізуємо проект заново → токени. Кеш із git-інвалідацією економить це. | — |
| **L1** | **Моніторинг змін законів** (weekly CRON → `watched_laws` → `needs_review` → алерт Olga) | 🔧 **Ops-автоматизація** — ✅ **вже побудовано** (cron на паузі) | Це scheduled job / «сенсор» harness, а не процедурне знання. | #2а |

**Відповідь на твоє питання прямо:** так — **новий сервіс (S1) робимо скілом**, це пріоритет №1.
Скілами стають **S1, S2, S3, S4, M1** (чисті dev-скіли). **S5** — гібрид (skill + рантайм-нода).
**L1 — не скіл**, а ops-автоматизація (CRON-сенсор); скіл можна додати лише щоб ним керувати.

> ⚠️ **Статус L1 (#2а):** вже реалізовано — `scripts/check-law-updates.mjs` + GitHub Actions
> `.github/workflows/law-monitor.yml` (weekly cron **тимчасово на паузі**, поки Olga у відпустці
> ~2 тижні з 2026-06-11; ручний запуск через Actions працює) + admin-панель «📋 Зміни законів»
> (`LawChangeLogPage.tsx`) + міграції 011/013. **Лишилось:** ввімкнути cron, коли рев'юер
> повернеться, + опційно LLM-самарі «що саме змінилось» (зараз ловиться зміна дати редакції).

### Структура скіла (приклад S1)
```
skills/new-service/
  SKILL.md          ← name + 1 рядок опису (progressive disclosure); тіло — кроки
  templates/        ← скелети form_config.json, document_template
  steps.md          ← завести services-рядок, міграцію, citations golden, parity-тест
```

### M1 — кеш аналізу проекту (memory-шар harness)

**Ідея:** на кожну область — файл аналізу з git-міткою. У новій сесії: збігається git-хеш
області → читаю кеш (дешево); змінилось або файла нема → аналізую заново й перезаписую.

**Чому git, а не mtime:** контейнер ефемерний, репо клонується щоразу заново → mtime скидається,
а `git log -1 --format=%H -- <path>` стабільний. Тому кеш **треба комітити в репо** (у `.gitignore`
він не переживе контейнер).

```
.claude/analysis/<area>.md
---
area: n8n/templates/
analyzed_at_commit: <sha>        # git log -1 --format=%H -- n8n/templates/
depends_on: [supabase/migrations/]   # кросс-зміни теж інвалідовують
---
<дайджест-указівник: що де лежить, ключові файли — НЕ дамп (правило no-duplication)>
```

**Правила, без яких ламається:** (1) ключ інвалідації = git-хеш + `depends_on`; (2) кеш = підказка,
**перевір 1–2 файли перед довірою** (мовчазно застарілий кеш гірший за відсутній); (3) короткі
дайджести-указівники, не копії доків. Повний розбір плюсів/мінусів — в історії чату/changelog.

---

## 🏗️ Частина 2. Harness: як зараз і як має бути

Harness = вся обвязка навколо моделі. Ось наші компоненти — **зараз** vs **ціль**.

| Компонент harness | Зараз | Має бути |
|-------------------|-------|----------|
| **Agent loop / оркестрація** | Лінійний n8n-флоу (детермінований) | ✅ Лишити. Агент-петля — тільки в ops-шарі, не в генерації |
| **Інструменти (tools)** | Groq, Google Docs API, Supabase | + RAG-retrieve (`search_law_chunks`) як інструмент для однієї секції |
| **Context delivery** | Форма дає всі дані (без retrieval) | + **вузький RAG** лише для «обґрунтування» (#2) |
| **Skills** | Немає формалізованих (тільки CLAUDE.md + session) | ✅ S1–S4 у репо (`skills/`) |
| **Verification / eval** | 263 parity-тести, citation golden (#36) — але **тільки build-time** | + **рантайм QA-нода** на згенерований документ → `quality_score` (#30) |
| **Critic / grounding** | Немає | + **агент-критик** після генерації: кожна `ст. X` має існувати в `law_chunks` (#2б) |
| **Sensors (датчики)** | ✅ **моніторинг законів** (#2а) — cron на паузі, ручний запуск працює | Ввімкнути weekly cron, коли рев'юер повернеться |
| **Permissions / sandbox** | AES-256, status kill-switch, без виконання коду | ✅ Лишити. DSL без `eval` — вже безпечно |
| **Memory / state** | `cases`, для dev — `session-summary.md` | + `quality_score` тренд у `cases`; + **кеш аналізу M1** (`.claude/analysis/`) |
| **Observability** | `cases.status` (мінімум) | + дашборд: тренд якості, citation-drift у CI |
| **Human-in-the-loop** | Заплановано, не живе | + панель ревʼю Olga для flagged-документів (#24) |

**Підсумок одним рядком:** ядро (детермінований шаблон) лишаємо як є — це сила. Додаємо навколо
нього три речі harness, яких бракує: **(1) сенсор законів**, **(2) рантайм-перевірку якості +
критик-grounding**, **(3) вузький RAG для обґрунтування**. Плюс формалізуємо **dev-скіли**.

---

## ✅ Частина 3. Чекліст (йди по порядку)

**Етап 1 — Dev-скіли (ризик 0, ROI одразу)**
- [ ] **S1** `skills/new-service/` — SKILL.md + templates + steps (головний пріоритет)
- [ ] **M1** `skills/project-analysis/` — кеш аналізу з git-інвалідацією (економить токени щосесії)
- [ ] **S2** `skills/citation-sync/` — обгорнути `scripts/extract-citations.mjs` (#36)
- [ ] **S4** `skills/gen-form-config/` — form_config зі списку вимог (#25)

**Етап 2 — Захист «рва» (юридична актуальність)**
- [x] **L1** Моніторинг законів — ✅ побудовано (`check-law-updates.mjs` + Actions + admin-панель) (#2а)
- [ ] **L1.1** Ввімкнути weekly cron, коли Olga повернеться (зараз закоментовано в `law-monitor.yml`)
- [ ] **L1.2** Опційно: LLM-самарі «що саме змінилось» (зараз ловиться лише зміна дати редакції)

**Етап 3 — Якість і RAG**
- [ ] **S5** Рантайм QA-нода + `quality_score` у `cases` (#30)
- [ ] **S3** `skills/add-law-chunks/` — індексація законів у `law_chunks` (#2)
- [ ] **Critic** Агент-критик grounding після генерації обґрунтування (#2б)
- [ ] **RAG** Вузький agentic-RAG лише для секції «обґрунтування» (#2)

**Етап 4 — Спостереження**
- [ ] HITL-панель Olga для flagged-документів (#24)
- [ ] Дашборд: тренд `quality_score`, citation-drift у CI

---

> Порядок не випадковий: спершу безпечні dev-скіли (вчимося патерну + знімаємо bottleneck),
> потім сенсор законів (захищає цінність), потім якість/RAG, потім спостереження.
